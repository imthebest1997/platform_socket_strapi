'use strict';

/**
 * Lifecycle callbacks for the `institution` model.
 */

module.exports = {
  /*
      lifeCycles before => name(event) {
        let { data, where, select, populate } = event.params;
      }
      lifeCycles after => name(event) {
        const { result, params } = event;
      }
    */
};
